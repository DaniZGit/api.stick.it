version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "./schema"
    gen:
      go:
        package: "database"
        sql_package: "pgx/v5"
        out: "generated/models"
        emit_json_tags: true
        # overrides:
        #   - db_type: "uuid"
        #     go_type:
        #       import: "github.com/google/uuid"
        #       type: "UUID"